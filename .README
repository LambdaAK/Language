expression ::= term + expression
    | term - expression
    | term



term ::= factor * term
    | factor / term
    | factor % term
    | factor


factor ::= <int>
    | ( expression )
    | - factor
    | factor ^ factor